@namespace("types")
protocol GenericExample {

  record Color {
    int red;
    int green;
    int blue;
    int alpha;
    union {
      string,
      null
    } name;
  }

  record Image {
    int width;
    int height;
    array<Color> colors;
  }
}
